# 参考区：外部仓库，不入版本控制（体积大且可重新克隆）
reference/

# 第三方内容：B 站视频的字幕与公开评论笔记，含他人内容，不随本仓库分发。
# 文件留在本地供调研使用，只是不进版本控制。
docs/NOTES-B站两期视频笔记.md

# 依赖与构建产物
node_modules/
dist/
build/
*.tsbuildinfo

# 参考仓库的编译产物（lobe-icons / lobe-ui 走构建流程）
.eslintcache
.turbo/
.changeset/*.md.bak

# 临时与日志
logs/
*.log
*.tmp

# 工具的写前保护副本（AOCI 在改 AGENTS.md 这类托管文件前会留一份）
*.backup.*
*.bak

# 凭证类（用完即删，绝不入库）
.env
.env.*
*cookies*
*credential*
*token*
# `*token*` 是广谱拦截，方向是对的（宁可多拦，不可漏拦）—— 但它顺带拦住了
# luzzy-page/src/styles/tokens.css，而那是设计 token，是 src/app/manifest.json 里
# 第一个必需样式。被拦住时**没有任何提示**：文件明明在，构建却少一个样式表，
# 而这件事要到克隆之后才会发现。所以给这一个文件开例外，而不是把广谱规则收窄 ——
# 收窄规则会让真正的凭证从此漏过。
!luzzy-page/src/styles/tokens.css

# Python 字节码缓存：从上游复制 skill 时被 -Recurse 带进来过一个 .pyc，不该进仓库
__pycache__/
*.pyc
